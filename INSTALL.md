<div dir="rtl">

# כספיון — מדריך התקנה

**כספיון** הוא לוח מחוונים לניהול תקציב המשפחה, שרץ **רק על המחשב שלכם**.
הנתונים הפיננסיים שלכם לא נשלחים לשום מקום — אין ענן, אין חשבון משתמש, אין מעקב.
המחשב מדבר רק עם הבנק שלכם עצמו, בדיוק כמו שדפדפן עושה.

---

## מה צריך לפני שמתחילים

| | |
|---|---|
| **מערכת הפעלה** | Windows 10 ומעלה (64 ביט), או macOS 13.5 ומעלה |
| **מקום פנוי בדיסק** | כ‑2 ג׳יגה |
| **אינטרנט** | דרוש להתקנה בלבד (הורדה של כ‑1.2 ג׳יגה) |
| **ידע טכני** | לא נדרש. אין צורך להתקין Python, Node או כל דבר אחר — ההתקנה מביאה הכול בעצמה |

---

## שלב 1 — הורדה

הורידו את קובץ ה‑ZIP מעמוד השחרורים (Releases), ופתחו אותו (Extract / חילוץ)
לתיקייה שנוח לכם — למשל שולחן העבודה או תיקיית המסמכים.

> חשוב: אל תריצו את הקבצים מתוך ה‑ZIP עצמו. קודם מחלצים, ואז מריצים.

---

## שלב 2 — התקנה (פעם אחת בלבד)

### ב‑Windows

לחצו לחיצה כפולה על **`install.bat`**.

בפעם הראשונה Windows יציג אזהרה כחולה — *"Windows protected your PC"*.
זה קורה לכל תוכנה שלא נרכשה חתימה דיגיטלית בתשלום, וזה לא אומר שיש בעיה:

1. לחצו על **More info** (מידע נוסף)
2. לחצו על **Run anyway** (הפעל בכל זאת)

### ב‑macOS

לחצו לחיצה כפולה על **`install.command`**.

בפעם הראשונה macOS יחסום את הקובץ כי הוא הורד מהאינטרנט. הפתרון:

1. לחצו על הקובץ עם **הכפתור הימני** (או Control + לחיצה)
2. בחרו **Open** (פתח)
3. בחלון שנפתח לחצו שוב על **Open**

צריך לעשות את זה רק בפעם הראשונה.

### מה קורה עכשיו

ייפתח חלון שחור עם טקסט רץ. ההתקנה לוקחת בערך **10 דקות** ומורידה כ‑1.2 ג׳יגה.
היא עוברת ארבעה שלבים:

```
[1/4] מוריד את מנהל ההתקנה
[2/4] מתקין את Python והספריות
[3/4] מתקין את רכיב סריקת הבנקים   ← השלב הארוך
[4/4] יוצר בסיס נתונים ריק
```

בסוף יופיע **"ההתקנה הושלמה"**. אפשר לסגור את החלון.

> אם ההורדה נקטעה באמצע (נפל האינטרנט, נסגר החלון) — פשוט הריצו את
> `install` שוב. הוא ממשיך מהנקודה שבה עצר ומנקה הורדות פגומות בעצמו.

---

## שלב 3 — הפעלה

מכאן והלאה, כדי לפתוח את כספיון:

- **Windows** — לחיצה כפולה על **`kaspion.bat`**
- **macOS** — לחיצה כפולה על **`kaspion.command`**

הדפדפן ייפתח אוטומטית עם לוח המחוונים.
**השאירו את החלון השחור פתוח** כל עוד אתם משתמשים בכספיון — סגירתו מכבה את התוכנה.

בהתחלה תראו מסך ריק עם ההודעה *"עדיין אין נתונים"*. זה תקין — עכשיו מוסיפים חשבון.

---

## שלב 4 — חיבור חשבון בנק או כרטיס אשראי

יש שתי דרכים להכניס נתונים.

### א. חיבור אוטומטי (מומלץ)

לחצו על **➕ הוספת חשבון** בתפריט הצדדי, בחרו את הבנק או חברת האשראי,
מלאו את פרטי ההתחברות ולחצו על הכפתור.

- הפרטים נבדקים מול הבנק **לפני** שהם נשמרים
- הם נשמרים **מוצפנים**, על המחשב שלכם בלבד
- מיד לאחר מכן נמשכים 90 הימים האחרונים והדף מתרענן

מכאן, כל לחיצה על **🔄 סנכרון עכשיו** מושכת את התנועות החדשות.

### ב. טעינת קובץ

שלושה גופים לא תומכים בחיבור אוטומטי ומופיעים באפור עם 🔒:

| גוף | למה | מה עושים |
|---|---|---|
| **ישראכרט** ו‑**אמריקן אקספרס** | הכניסה לאתר חסומה מפני תוכנות | מורידים את פירוט העסקאות (Excel) מהאתר |
| **ONE ZERO** | דורש אימות דו‑שלבי חד־פעמי | מורידים את הקובץ מהאפליקציה |

את הקובץ טוענים במסך **תנועות** ← *"הוספת הוצאה · טעינת קובץ"* ← בחירת קובץ ← **טעינה**.
הגוף מזוהה אוטומטית מתוך הקובץ, וטעינה חוזרת של אותו קובץ **לא** תיצור כפילויות.

> שימו לב: סורק של בנק קורא את **חשבון העו״ש בלבד**. אם הכרטיס שלכם מונפק על ידי
> חברה אחרת (הכרטיסים של הבינלאומי מגיעים מכאל) — הוסיפו גם את חברת האשראי כחשבון
> נפרד, אחרת הכרטיס יופיע רק כחיוב חודשי אחד גדול במקום פירוט קניות.

---

## איפה הנתונים שלי נשמרים?

**לא** בתיקיית התוכנה — אלא בתיקייה האישית שלכם במערכת ההפעלה:

| מערכת | תיקייה |
|---|---|
| macOS | `~/Library/Application Support/kaspion/` |
| Windows | `%LOCALAPPDATA%\kaspion\` |

הקובץ `finance.duckdb` הוא כל המידע שלכם — **זה הקובץ שכדאי לגבות**.
לצידו נשמרים פרטי ההתחברות לבנק, מוצפנים.

הפרדה זו גם אומרת שכל מחשב מתחיל עם בסיס נתונים ריק משלו — שום מידע לא עובר בין מחשבים.

---

## הסרת התוכנה

מוחקים שתי תיקיות:

1. תיקיית התוכנה (זו שחילצתם מה‑ZIP)
2. תיקיית הנתונים מהטבלה למעלה

זהו. שום דבר לא הותקן במקום אחר במחשב, ולא נדרשה הרשאת מנהל בשום שלב.

---

## תקלות נפוצות

| מה קורה | מה לעשות |
|---|---|
| החלון נסגר מיד / ״לא הותקן״ | לא הרצתם עדיין את `install`, או שההתקנה נכשלה. הריצו אותה שוב. |
| ההתקנה נעצרה באמצע | הריצו את `install` שוב — הוא ממשיך מאותה נקודה. |
| ״שם משתמש או סיסמה שגויים״ | בדקו את הפרטים באתר הבנק עצמו. אחרי כמה ניסיונות בנקים חוסמים חשבון — היזהרו. |
| הדפדפן לא נפתח לבד | פתחו אותו ידנית בכתובת `http://127.0.0.1:8765` |
| ״לא ניתן לבנות את הדשבורד״ | ההתקנה לא הושלמה. הריצו את `install` שוב. |
| הבנק דורש קוד SMS בכל כניסה | הסורק לא יכול לעבור אימות דו‑שלבי — השתמשו בטעינת קובץ. |

---

## הערה חשובה

משיכת נתונים אוטומטית מאתר הבנק עשויה לעמוד בניגוד לתנאי השימוש של הבנק שלכם.
השתמשו בחשבונות שלכם בלבד, על המחשב שלכם, על אחריותכם.
זהו כלי אישי — לא תוכנה פיננסית מוסמכת. בדקו נתונים חשובים מול הדוחות הרשמיים של הבנק.

</div>
