# ─── personal data — NEVER commit ───────────────────────────
# the DuckDB database, encryption key, credentials
data/
!data/.gitkeep
*.duckdb
*.duckdb.wal
# generated dashboard — contains your real transactions
dashboard.html
# encrypted credential blobs + keys
*.enc
.kaspion_key
.env

# ─── generated / local tooling ──────────────────────────────
__pycache__/
.venv/
node_modules/
# created by the installers: vendored uv, and the Chromium the scraper drives
.tools/
.puppeteer/
*.egg-info/
.ruff_cache/
evals/last_run.json
dbt/target/
dbt/dbt_packages/
dbt/logs/
dbt/.user.yml
logs/
.DS_Store
